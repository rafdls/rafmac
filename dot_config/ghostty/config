# UI
# Matches Neovim's tokyonight so ANSI colours line up in :terminal, tmux and CLIs.
# `background` below is set after this deliberately: it overrides the theme's
# #1a1b26 back to pure black, which reads better under background-blur.
theme = TokyoNight Night
font-size = 16
font-family = "JetBrains Mono Nerd Font"
font-thicken = true
font-thicken-strength = 1
adjust-cell-height = 1
background-opacity = 0.85
background-blur=16
background=#000000
macos-titlebar-style = hidden
window-padding-x = 8
window-padding-y = 8


# Fix reisizing pane with tmux
macos-option-as-alt = left
keybind = alt+arrow_left=unbind
keybind = alt+arrow_right=unbind

# Quality of life
mouse-hide-while-typing = true

# scrollback-limit in bytes, roughly 125,000 lines
scrollback-limit = 10000000
